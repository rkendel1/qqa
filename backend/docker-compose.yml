services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"  # Maps 'host.docker.internal' to the host machine
    environment:
      - PYTHONUNBUFFERED=1
